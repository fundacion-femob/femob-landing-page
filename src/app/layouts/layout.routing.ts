import { Routes } from '@angular/router';

import { LayoutOneComponent } from './layout-one/layout-one.component';

export const LayoutRoutes: Routes = [
  { path: 'home', component: LayoutOneComponent },
];